const LoginHero = () => (
  <section
    className="auth-hero"
    role="region"
    aria-labelledby="login-hero-title"
  >
    <div className="auth-hero-content">
      <h1 id="login-hero-title">Welcome Back</h1>
      <p>Login to continue your Little Lemon experience</p>
    </div>
  </section>
);

export default LoginHero;
